<!DOCTYPE html>
<html data-theme="<%= current_user&.theme || 'default' %>">
  <head>
    <title><%= content_for(:title) || "Wrap" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Wrap">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.ico" type="image/ico">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="nb-nav" data-controller="mobile-nav">
      <div class="nb-container">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-3">
            <img src="/icon.png" alt="Wrap" class="w-12 h-12 rounded-lg shadow-sm">
            <div>
              <p class="text-xs font-black uppercase tracking-widest">Wrap</p>
            </div>
          </div>

          <!-- Hamburger button - mobile only -->
          <button type="button" 
                  class="md:hidden nb-btn nb-btn--white p-2"
                  data-action="click->mobile-nav#toggle"
                  aria-label="Toggle menu">
            <svg data-mobile-nav-target="openIcon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
            <svg data-mobile-nav-target="closeIcon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>

          <!-- Desktop nav -->
          <nav class="hidden md:flex items-center gap-2">
            <%= link_to "Today", dashboard_path, class: "nb-btn nb-btn--white" %>
            <%= link_to "Habits", habits_path, class: "nb-btn nb-btn--white" %>
            <% if authenticated? %>
              <%= link_to "Settings", settings_path, class: "nb-btn nb-btn--white" %>
              <%= button_to "Sign out", session_path, method: :delete, class: "nb-btn nb-btn--white text-gray-600" %>
            <% end %>
          </nav>
        </div>

        <!-- Mobile nav dropdown -->
        <nav data-mobile-nav-target="menu" 
             class="hidden flex-col gap-2 pt-4 border-t-2 border-black mt-4 md:hidden">
          <%= link_to "Today", dashboard_path, class: "nb-btn nb-btn--white w-full justify-center", data: { action: "click->mobile-nav#close" } %>
          <%= link_to "Habits", habits_path, class: "nb-btn nb-btn--white w-full justify-center", data: { action: "click->mobile-nav#close" } %>
          <% if authenticated? %>
            <%= link_to "Settings", settings_path, class: "nb-btn nb-btn--white w-full justify-center", data: { action: "click->mobile-nav#close" } %>
            <%= button_to "Sign out", session_path, method: :delete, class: "nb-btn nb-btn--white w-full justify-center text-gray-600" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="nb-container">
      <div id="flash" class="mb-6">
        <%= render "shared/flash" %>
      </div>

      <%= yield %>
    </main>
  </body>
</html>
